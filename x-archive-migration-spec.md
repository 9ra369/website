# Xポスト → サイト記事 移行仕様書

## 1. 目的

これまでXで発信してきた情報資産を、自サイトに記事として集約する。
X APIは利用せず、公式アーカイブ機能で取得したデータを機械変換し、取捨選択と仕上げを人間が担う。

### 方針

- **全自動にはしない。** 機械変換で並べるところまでを自動化し、「捨てる」「磨く」は手作業
- 削除は行わず、不要なものは別フォルダへ**移動**する（不可逆な操作を避ける）
- 確認作業を「本文を読む」から「項目を照合する」に落とし込む

---

## 2. 全体フロー

```
[1] Xアーカイブ申請・ダウンロード（手動 / PCブラウザ）
      ↓
[2] tweets.js → 正規化JSON（スクリプト）
      ↓
[3] 振り分け・スレッド結合（スクリプト）
      ↓
[4] title / summary / tags 生成（AI）
      ↓
[5] Markdown書き出し（スクリプト）
      ↓
[6] 一覧確認・アーカイブ送り（手作業）
      ↓
[7] 公開分の仕上げ（手作業）
```

---

## 3. 入力データ

### 取得方法

Xの設定 →「アカウント」→「データのアーカイブをダウンロード」から申請。

| 項目 | 内容 |
|---|---|
| 費用 | 無料 |
| 準備時間 | 最大24時間程度 |
| 期限 | 完了通知から約1週間 |
| 実行環境 | **PCブラウザ推奨**（ZIPが数GBになる場合があるため） |

### 対象ファイル

`data/tweets.js`

先頭の変数代入部分を除去すればJSONとしてパース可能。

### 使用するフィールド

| フィールド | 用途 |
|---|---|
| `id_str` | 一意キー、元ポストURLの生成 |
| `full_text` | 本文 |
| `created_at` | 日付 |
| `in_reply_to_status_id` | リプライ判定 / スレッド結合 |
| `in_reply_to_user_id` | 自己リプライ（スレッド）か他人宛かの判別 |
| `entities.urls[].expanded_url` | t.co展開済みの元URL |
| `entities.urls[].url` | 本文中のt.coリンク置換用 |
| `entities.hashtags` | タグ候補 |
| `entities.user_mentions` | 実質文字数の算出時に除外 |
| `extended_entities.media` | 添付画像の紐付け |

---

## 4. 振り分けルール

以下の順に評価し、最初にマッチした時点で確定する。

| 優先 | 条件 | 振り分け先 |
|---|---|---|
| 1 | `full_text` が `RT @` で始まる（リツイート） | `_archive/` |
| 2 | 他人へのリプライ（`in_reply_to_user_id` が自分以外） | `_archive/` |
| 3 | `expanded_url` を1つ以上持つ | **`posts/`**（文字数不問） |
| 4 | 実質文字数 < N | `_archive/` |
| 5 | 上記以外 | **`posts/`** |

### 補足

- **優先3の意図**: 短くてもURL付きのポストは「一行紹介＋リンク」であり、情報サイトとしては資産。文字数で切り落とさない。リンク集ページの素材にもなる
- **実質文字数の定義**: `full_text` から URL・メンション・ハッシュタグ・前後の空白を除去した後の文字数
- **閾値N**: 暫定 40〜60字。実データの文字数分布をヒストグラムで確認してから確定する。**閾値を変えて何度でも振り分け直せる実装にすること**

---

## 5. スレッド結合

自己リプライで連なる連続ポストは、**1記事に結合**する。

- 判定: `in_reply_to_user_id` が自分自身
- 結合順: `id_str` の昇順（時系列）
- 記事のメタ情報（日付・IDなど）は**スレッド先頭のポスト**のものを採用
- 結合後の記事に対して、改めて振り分けルールを適用する

---

## 6. 記事仕様

### ディレクトリ構成

```
content/
├── posts/       # 本フォルダ（記事候補）
├── _triage/     # 判断保留
└── _archive/    # 対象外（削除はしない）
```

### ファイル名

```
YYYY-MM-DD_タイトル冒頭30字.md
```

例: `2024-03-15_HoudiniのVEXでカーブに沿って.md`

**意図**: ファイルを開かずに一覧表示だけで取捨選択できるようにする。ここが確認作業の効率を最も左右する。

### フロントマター

```yaml
---
title:          # AI生成
date:           # created_at から機械変換
category:       # 分類（後述）
tags: []        # AI生成 + 元ハッシュタグ
source_url:     # 紹介先URL（expanded_url、なければ空）
original_post:  # 元ポストのURL
summary:        # 2〜3行、AI生成
status: draft   # draft / published
---
```

### フィールド規約

- **空欄を許容する。** AIが埋められなかった項目は推測で埋めず、必ず空にする。推測値は確認コストの温床になる
- `original_post` は**必ず保持する**。原文へ戻れる状態を維持することで、安心して自動処理を任せられる
- `status` は初期値 `draft`。人間が仕上げたものだけ `published` に変更する

### 本文

- t.coリンクは `expanded_url` に置換する
- 改行はそのまま保持
- 添付画像がある場合は参照を記載

---

## 7. AI生成ルール

AIが担当するのは以下の3項目のみ。

| 項目 | 内容 |
|---|---|
| `title` | 本文から生成 |
| `summary` | 2〜3行の要約 |
| `tags` | 内容から付与 |

### 制約

- **本文に存在しない固有名詞をタイトルに含めない。** 元ポストに見出しが無い以上タイトルはAI生成に依存するため、ここが唯一ハルシネーションの混入しうる箇所
- 判断できない場合は空欄で返す（推測しない）
- 確信度を併せて出力させ、**低いものだけを精読対象**とする

---

## 8. 手作業フェーズ

### 8-1. 取捨選択

- `posts/` をファイル名の一覧で確認し、不要なものを `_archive/` へ移動
- **削除はしない**

### 8-2. 仕上げ

- **生き残った全件を磨こうとしない。** 全件仕上げを前提にすると途中で頓挫する
- カテゴリ単位で「これは出す」と決めたものから着手し、完了したら `status: published` に変更
- 残りは `draft` のまま保持

---

## 9. サイト側の設計上の留意点

1ポスト1記事のため、1記事あたりの分量は数十〜百数十字程度になる。

- 個別ページは「記事」ではなく**ノート／メモ**として扱う
- 閲覧の入口はカテゴリ別インデックスページとし、一覧性でサイトの価値を担保する
- レイアウトは記事型ではなく**カード型・メモ型**にすることで、1件あたりの短さが不自然に見えないようにする

---

## 10. 未決事項

- [ ] 実質文字数の閾値 N の確定（文字数分布の確認後）
- [ ] `category` の分類体系の確定
- [ ] 添付画像の扱い（サイトへ移行するか、元ポストへのリンクで済ませるか）
- [ ] サイト名の確定
