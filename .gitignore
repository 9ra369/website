# dependencies
node_modules/

# build output
dist/
.output/
.astro/

# env
.env
.env.*
!.env.example

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# misc
*.local

# raw personal data exports (X archive, etc.) — never commit
_raw/

# intermediate pipeline output derived from _raw — pre-curation, not for commit
_work/

# local dev tooling (ffmpeg binary for video->GIF thumbnails) — not for commit
.tools/
